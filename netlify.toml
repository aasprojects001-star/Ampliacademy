[build]
  base = "."            # Use project root
  command = "npm run build"
  publish = "dist"      # Output folder from build

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200